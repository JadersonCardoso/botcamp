import styled from 'styled-components'

import iconLogout from '../../imgs/iconfinder_logout_4115235.png'

const IconLogout = styled.img.attrs({
    src: iconLogout,
    alt: 'Icon logout chat'
})`
    width: 30px;
    height: 30px;
    object-fit: contain;
`
export default IconLogout